# This is the sshd server system-wide configuration file. See
# sshd_config(5) for more information.

# This file provides defaults for the system on startup.
# The values can be changed at system or service reload without a restart.

# To enable root login via SSH, change the following line to 'yes':
PermitRootLogin yes

# Other settings:
Include /etc/ssh/sshd_config.d/*.conf

# Standard Port, LogLevel, and other initial configuration
Port 22
LogLevel INFO
LoginGraceTime 2m
PermitEmptyPasswords no
ChallengeResponseAuthentication no

# Change to no to disable tunnelled clear text passwords
PasswordAuthentication yes

# Depending on system policy, you might also want to configure:
PubkeyAuthentication yes
